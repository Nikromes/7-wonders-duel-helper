# Инструкция по настройке эффектов карточек (Значки vs Текст)

В файле `data.js` вы задаете свойства всех карточек игры. То, что карточка **даёт** игроку (её эффект), записывается либо в поле `desc` (для чудес, гильдий, жетонов), либо в поле `type` (для карточек из колод эпох).

Скрипт автоматически анализирует текст в этих полях и сам решает: нарисовать **иконки** ресурса/щита/победных очков, или же вывести **текстовое описание**, если эффект сложный.

Ниже описаны точные правила, по которым скрипт распознает текст и заменяет его на картинки.

## 1. Точные текстовые совпадения (Базовые ресурсы)

Если строка эффекта **точно совпадает** (регистр букв не важен) с одним из слов ниже, скрипт нарисует соответствующую иконку (или две иконки). Никаких других символов, пробелов или пояснений в строке быть не должно.

*   `Дерево` ➔ 🪵 (1 деревянная иконка)
*   `Глина` ➔ 🧱 (1 глиняная иконка)
*   `Камень` ➔ ⛏️ (1 каменная иконка)
*   `Стекло` ➔ 🥃 (1 стеклянная иконка)
*   `Папирус` ➔ 📜 (1 иконка папируса)
*   `Дерево x2` ➔ 🪵🪵 (2 деревянные иконки)
*   `Глина x2` ➔ 🧱🧱 (2 глиняные иконки)
*   `Камень x2` ➔ ⛏️⛏️ (2 каменные иконки)

*Пример в `data.js`:*
```javascript
{ id: "a1_br1", title: "Лесозаготовка", type: "Дерево", color: "brown", ... }
```

## 2. Число + Ключевое слово (Щиты, Очки, Монеты)

Если карточка даёт определенное количество военных щитов, победных очков или монет, используйте строгий формат: **Цифра + Пробел + Слово**. Скрипт распознает эту конструкцию и нарисует красивый значок с указанной цифрой (или столько щитов, сколько указано).

**Допустимые форматы слов:**
*   **Щиты:** `щит`, `щита`, `щитов`
*   **Победные Очки:** `ПО` (только так)
*   **Монеты:** `монет`, `монеты`, `монета`

*Примеры идеальных строк для иконок:*
*   `2 Щита` ➔ Нарисует две иконки красных щитов 🛡️🛡️
*   `5 ПО` ➔ Нарисует синий круглый значок Победных Очков с цифрой 5 внутри
*   `6 Монет` ➔ Нарисует золотую монетку с цифрой 6 внутри

*Пример в `data.js`:*
```javascript
{ id: "a2_rd1", title: "Стрельбище", type: "2 Щита", color: "red", ... }
{ id: "a2_bl1", title: "Акведук", type: "5 ПО", color: "blue", ... }
{ id: "a2_yl1", title: "Пивоварня", type: "6 Монет", color: "yellow", ... }
```

## 3. Вывод обычным текстом (Сложные эффекты)

Если вы напишете любую строку, которая **не подходит** под правила 1 и 2, скрипт оставит этот текст как есть и красиво выведет его белым шрифтом вверху карточки.

Это используется для карточек науки, гильдий, особых желтых карт коммерции и сложных условий, для которых пока нет отдельных иконок.

*Примеры строк, которые останутся текстом:*
*   `Наука (Колесо)` (не точное совпадение ресурса)
*   `3 Монеты/Товара, 2 ПО/Товара` (есть лишние символы: слэш, слова "Товара")
*   `Гильдия` (не базовое слово-ресурс)
*   `Товар (Стекло/Папирус)` (описание сложного эффекта)
*   `Приносит деньги за карточки ресурсов противника.` (длинное описание для Жетона)

*Пример в `data.js`:*
```javascript
{ id: "a3_yl5", title: "Коммерческая палата", type: "3 Монеты/Товара, 2 ПО/Товара", color: "yellow", ... }
```

---
**Итог:** Редактируя `data.js`, просто пишите эффекты так, как вам удобно. Если вы соблюдаете лаконичный формат (`3 ПО`, `Дерево x2`), система сама всё украсит иконками. Если вам нужно расписать логику работы карточки текстом — пишите текстом, код ничего не сломает.
