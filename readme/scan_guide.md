# Руководство по AI-сканированию стола

## Как это работает

1. Откройте **Калькулятор Эпох** → выберите нужную эпоху
2. Нажмите кнопку **📷** (камера)  
3. Сфотографируйте стол или выберите фото из галереи
4. AI проанализирует фото и определит открытые карты
5. Обнаруженные карты автоматически отметятся в калькуляторе
6. Появится модалка с резюме: найденные ✅ и ненайденные ❓ карты

## Настройка API-ключа

1. Нажмите **⚙️** в правом верхнем углу сайта
2. Вставьте свой Gemini API-ключ → **Сохранить**
3. Ключ хранится в `localStorage` вашего браузера — вводится один раз
4. [Получить ключ бесплатно →](https://aistudio.google.com/apikey)

## Как сделать фото для лучшего распознавания

- Фотографируйте **сверху** под прямым углом
- Убедитесь что карты **хорошо освещены**
- Чем больше видно названий карт — тем точнее результат
- AI определяет частично закрытые карты по цвету, стоимости и эффекту

## Файлы сканирования

| Файл | Для чего |
|------|----------|
| `scan.js` | Вся логика: выбор фото, API-запрос, сопоставление карт, отображение результатов |
| `scan-prompt.js` | AI промпт — можно редактировать для улучшения точности |
| `ai-config.js` | Модель AI (по умолчанию `gemini-3-flash-preview`) |
| `api-key.js` | Управление ключом (localStorage + фолбэк) |

## Изменение модели AI

В файле `ai-config.js`:
```javascript
const AI_CONFIG = {
    model: 'gemini-3-flash-preview', // Изменить на другую модель при необходимости
    getEndpoint() { ... }
};
```

## Отладка

В консоли разработчика (F12) при каждом сканировании выводится:
- `📷 Scan — данные для AI` — эпоха и полные данные карт отправленные в AI
- `Gemini raw response` — сырой ответ AI
- Результаты сопоставления — найденные и ненайденные карты
